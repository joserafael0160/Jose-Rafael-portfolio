---
const PROJECTS = [
  {
    title: "Project 1",
    description: "lrem ipsum kafljklafjkljdlkfjakjfklajfk  afkldjklajf ad",
    link: "#",
    github: "#",
    image: "https://via.placeholder.com/300x200",
    tags: ["HTML", "PostCss", "Javascript"]
  }
]
---

{
  PROJECTS.map(({ image, title, description, tags}) => (
    <article>
      <h3>{title}</h3>
      <p>{description}</p>
      <ul>
        {tags.map(tag => (
          <li>{tag}</li>
        ))}
      </ul>
      <img src={image} alt={`Screenshot of the project ${image}`} />
    </article>
  ))
}